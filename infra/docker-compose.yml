services:
  web:
    build:
      context: ..
      dockerfile: apps/web/Dockerfile
    container_name: expense-manager-web
    env_file:
      - ../apps/web/.env.local
    ports:
      - '3000:3000'
    restart: unless-stopped

